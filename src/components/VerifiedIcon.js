import React from 'react';
import VerifiedIcon from '@mui/icons-material/Verified';

/**
 * A simple wrapper around the MUI VerifiedIcon
 */
export default function VerificationIcon(props) {
  return <VerifiedIcon {...props} />;
}
